import { WikiFormatPipe } from './wiki-format-pipe';

describe('WikiFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new WikiFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
